[package]
name = "errors"
version = "0.1.0"
edition = "2024"

[dependencies]
# Internal

# External
smol_str = "0.3.6"
smallvec = { version = "1.15.1", features = ["union"] }
derive_more = { version = "2.1.1", features = ["display", "error"] }

[lints]
workspace = true
