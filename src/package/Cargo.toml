[package]
name = "package"
version = "0.1.0"
edition = "2024"

[dependencies]
# Internal
# External
slotmap = "1.1.1"
derive_more = { version = "2.1.1", features = ["display", "error", "from"] }

[lints]
workspace = true
