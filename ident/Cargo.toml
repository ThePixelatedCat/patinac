[package]
name = "ident"
version = "0.1.0"
edition = "2024"

[dependencies]
# Internal

# External
string-interner = "0.20.0"
derive_more = { version = "2.1.1", features = ["display"] }

[lints]
workspace = true
