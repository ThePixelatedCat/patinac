def main(): () = {
    let single_if: () = if true { false () }
    let full_if: Float = if true { 5.0 } else { -3.0 }
    let inc: Fn(mut UInt) -> () = fn(mut a) -> a = a + 1
    let ints: UInt = { 
        let a = 5 
        let b: UInt = 1 
        a + b 
    }
    let shadowing: Float = {
        let a: UInt = 5
        let a = 6.0
        { let a = true }
        a
    }
    let int_fallback = 5
}