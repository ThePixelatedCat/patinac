type Box(v: Int)
def field(): () = {
    let f = fn(mut a, b: Box) -> { a = a + b.v }
    let g = fn(mut a) -> f(mut a.v, a)
}

def same_swap(): () = {
    let mut a = 1
    swap(mut a, mut a)
}

def swap(mut a: Int, mut b: Int): () = {}