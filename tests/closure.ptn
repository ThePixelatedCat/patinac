def main(): () = {
    let f = make_closure
    let closure = f()
    // add assert later
    print apply(closure, 2)
}

def make_closure(): Fn(UInt) -> UInt = {
    let x = 1
    fn(y) -> x + y
}

def apply(f: Fn(UInt) -> UInt, v: UInt): UInt = f(v)
