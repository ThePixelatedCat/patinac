def main(): () = {
    let f = make_closure
    let closure = f()
    // add assert later
    print apply(closure, 2)
}

def make_closure(): Fn(UInt) -> Int = {
    let array: [Int] = [1, 2, 3, 4, 5]
    fn(index) -> array.[index]
}

def apply(f: Fn(UInt) -> Int, v: UInt): Int = f(v)
