type Point(x: Float, y: Float)

def get_x(this: Point): Float = this.x

def make_point(x: Float): Point = Point(x, 0.0)

def main(): () = {
    let point = make_point(1.0)
    get_x(point)
    // add assert in future
    print point == Point(1.0, 0.0)
}