fn sum(a: Int, b: Int): Int -> a + b
fn sum_block(a: Int, b: Int): Int -> {
    a + b
}
const SUM = sum

fn square(i: Int): Int -> i * i
// fn sumOfSquares(n: UInt): Int -> range(0, n).iter().map(square).fold(0, sum)

const TWELVE: Int = 12
const INC = fn(i) -> sum(1, i)

record Point(x: Int, y: Int)

// Can be initialised as either of the following:
// const order_init = Point(0, 0)
// const label_init = Point(x: 0, y: 0)